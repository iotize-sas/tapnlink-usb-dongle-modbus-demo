import { version } from "../../package.json";

export const environment = {
  production: true,
  appName: "Modbus Slave Demo",
  version: version,
  debug: false,
  nfcMimeType: "application/com.mycompany.appgagi"
};
